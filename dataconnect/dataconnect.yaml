specVersion: "v1"
serviceId: "angular-portfolio-starter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-portfolio-starter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
